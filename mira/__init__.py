"""Mira — Multi-agent Insight & Reasoning Architecture.

A reusable, product-agnostic agentic-AI framework: a deepagents sub-agent research
panel over a local LLM (Ollama) that reads data through a pluggable DataProvider and
emits a structured, advisory InsightReport. Products plug in via a config + provider
mapping; the framework knows nothing about any specific product.
"""
__version__ = "0.1.0"
