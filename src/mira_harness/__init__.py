"""Agent-agnostic governance & improvement planes over the public contracts (ADR-050).

Policy detectors, cost attribution, trace scoring, the generic regression
gate, eval-gated versioning, and the ADR-051 foreign-agent adapters. May
import ``mira_contracts`` only — never ``mira.*`` (lint-enforced direction).
"""
